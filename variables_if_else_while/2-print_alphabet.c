#include <stdio.h>

/**
 * main - prints the alphabet with putchar
 * return: 0 if success
 */

int main(void)
{
	char m;
	char M;

	for (m = 'a'; m <= 'z'; a++)
		putchar(m);
	for (M = 'A'; M <= 'Z'; A++)
		putchar(M);
	

	putchar('\n');
	return (0);
}
