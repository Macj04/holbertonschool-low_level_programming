#include "main.h"
/**
 * _islower - checks for lowercase a character
 * @c: a character
 * Return: 1 is a lower, 0 is not
 */
int _islower(int c)
{
	/*this function determines if c is lowercase or not*/
	return (c >= 'a' && c <= 'z');
}
